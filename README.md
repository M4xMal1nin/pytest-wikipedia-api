<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>Pytest Wikipedia API Autotests 🚀</title>
</head>
<body>

<h1>Pet-project: API Autotests for Wikipedia (MediaWiki API) 🚀</h1>

<p>
    В рамках данного pet-проекта был разработан набор API-автотестов для публичного
    <strong>Wikipedia (MediaWiki) API</strong> с использованием
    <strong>Python 🐍, pytest 🧪 и requests 🌐</strong>.
</p>

<p>
    Проект выполнен с применением production-подходов и демонстрирует практические навыки
    автоматизации API-тестирования, корректной организации тестовой инфраструктуры,
    а также работы с pytest fixtures (да, без copy-paste 😌).
</p>

<hr>

<h2>🔥 Ключевые особенности проекта</h2>

<ul>
    <li>Чистая и масштабируемая структура проекта (без хаоса и магии ✨)</li>
    <li>Использование <strong>pytest</strong> как основного тестового фреймворка 🧪</li>
    <li>Переиспользуемые <strong>fixtures</strong> — один раз написал, много раз используешь ♻️</li>
    <li>Вынос базовых параметров, заголовков и HTTP-клиента в <code>conftest.py</code></li>
    <li>
        Учет реальных ограничений Wikipedia API
        (<code>User-Agent</code> обязателен — иначе 403 и грусть 😅)
    </li>
    <li>Позитивные и негативные сценарии тестирования (happy path и жизнь 💥)</li>
    <li>Корректная настройка <code>.gitignore</code> (никаких <code>venv</code> в репозитории 🙅‍♂️)</li>
    <li>README с пошаговой инструкцией «склонировал → запустил → работает» ✅</li>
</ul>

<p>
    Проект полностью воспроизводим: любой желающий может
    <strong>склонировать репозиторий</strong> и
    <strong>запустить автотесты локально</strong> с помощью
    <code>git clone</code> и <code>pytest</code>.
    Магии нет — только код 🧙‍♂️❌
</p>

<hr>

<h2>🧪 Список реализованных тестов</h2>

<ul>
    <li>
        <strong>API availability test</strong><br>
        Проверка того, что Wikipedia API жив, бодр и отвечает статусом
        <code>200 OK</code> (пульс есть ❤️).
    </li>

    <li>
        <strong>Response format validation</strong><br>
        Проверка, что API возвращает JSON, а не HTML с неожиданным сюрпризом 😬.
    </li>

    <li>
        <strong>Positive search scenario</strong><br>
        Проверка, что поиск по валидному запросу
        (<em>Python</em>) действительно что-то находит 🐍📚.
    </li>

    <li>
        <strong>Response structure validation</strong><br>
        Проверка структуры ответа и наличия обязательного поля
        <code>title</code> (порядок должен быть ☝️).
    </li>

    <li>
        <strong>Negative search scenario</strong><br>
        Проверка, что поиск по несуществующему запросу
        возвращает пустой список, а не фантазии API 🤡.
    </li>
</ul>
